//JSON.stringify with predefined delimeter

const obj = {
    fname: "Rahul",
    lname: "Ray"
}

let result = JSON.stringify(obj);
result = result.replace()

console.log(result,typeof(result),typeof(obj));